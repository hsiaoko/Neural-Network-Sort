cmake_minimum_required(VERSION 3.2)
PROJECT(nnsort)
INCLUDE_DIRECTORIES(include)
AUX_SOURCE_DIRECTORY(src DIR_SRCS)

SET(NNS ${DIR_SRCS})
SET(CMAKE_CXX_STANDARD 11)
ADD_EXECUTABLE(${PROJECT_NAME} ${NNS})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} m)